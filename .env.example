    # Flask App Configuration
    FLASK_APP=run.py
    FLASK_ENV=development

    # Secret key for session management, CSRF protection, etc.
    # IMPORTANT: Replace with a strong, random secret key in your .env file!
    SECRET_KEY='your-strong-secret-key'

    # Database Configuration
    # Using SQLite for simplicity in this example
    SQLALCHEMY_DATABASE_URI='sqlite:///../instance/app.db'

    # Add other environment variables as needed (e.g., API keys, email settings)